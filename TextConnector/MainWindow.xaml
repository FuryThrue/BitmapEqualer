<Window x:Class="BitmapEqualer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BitmapEqualer"
        mc:Ignorable="d"
        Title="MainWindow" ResizeMode="NoResize" Height="100" Width="400">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="FirstFileButton" Content="Выбрать первый файл" HorizontalAlignment="Center" VerticalAlignment="Center" Click="FirstFile_onClick"/>
        <Button x:Name="SecondFileButton" IsEnabled="False" Content="Выбрать второй файл" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SecondFile_Click" Grid.Column="1"/>

        <CheckBox x:Name="IsSuccesCheckBox" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="1" IsEnabled="False"/>
    </Grid>
</Window>
